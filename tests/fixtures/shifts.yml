shift1:
  slug: shift1
  start: !php/eval return($now);
  end: !php/eval return($now + 1*$secs_per_hour);

shift2:
  slug: shift2
  start: !php/eval return($now);
  end: !php/eval return($now + 2*$secs_per_hour);

